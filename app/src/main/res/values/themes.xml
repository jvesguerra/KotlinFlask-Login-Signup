<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.Portal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="android:popupBackground">@color/colorPopupBackground</item>
    </style>

    <!-- Custom light theme that inherits from Base.Theme.Portal -->
    <style name="Base.Theme.Portal.Light" parent="Base.Theme.Portal">
        <!-- Additional customizations for light mode -->
    </style>

</resources>